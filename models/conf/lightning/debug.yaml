defaults:
  - base

trainer:
  log_every_n_steps: 5
  profiler: "simple"
  limit_train_batches: 0.02
  limit_val_batches: 0.02
  limit_test_batches: 0.02
  num_sanity_val_steps: 2
model_checkpoint:
  save_top_k: 1
