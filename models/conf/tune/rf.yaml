defaults:
  - cpu_base

# Max num trials = 20 * num_hyperparameters (2 for RF model tuning)
# Stopper must reach plateau objective for 3 consecutive trials
params:
  num_trials: 40
stopper:
  std: 0.001
  patience: 3
