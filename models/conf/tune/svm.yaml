defaults:
  - cpu_base

# Max num trials = 20 * num_hyperparameters (1 for SVM model tuning)
# Stopper must reach plateau objective for 3 consecutive trials
params:
  num_trials: 20
stopper:
  std: 0.001
  patience: 3
